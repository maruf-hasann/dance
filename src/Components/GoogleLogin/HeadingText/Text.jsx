import React from 'react';

const Text = ({text}) => {
    return (
        <div>
            <h1 className='font-bold text-4xl text-center mb-14'>{text}</h1>
        </div>
    );
};

export default Text;