import React from 'react';

const Text = ({text}) => {
    return (
        <div>
            <h2 className='font-bold text-4xl text-center mb-14'>{text}</h2>
        </div>
    );
};

export default Text;